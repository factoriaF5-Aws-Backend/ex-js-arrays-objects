/**
 * Devuelve el numero mas grande de una array.
 * @param {numbers} array - una array de numeros.
 * @returns {number} - Retorna el numero mas grande dentro de la array.
 */
function findLargestNumber(numbers) {}

/**
 * Devuelve una array de numeros pares.
 * @param {numbers} array - una array de numeros.
 * @returns {array} - Retorna una array con los numeros pares de la array "numbers".
 */
function filterEvenNumbers(numbers) {}

/**
 * Devuelve una array inversa.
 * @param {arr} array - una array.
 * @returns {array} - Retorna una array inversa en orden de la array "arr".
 */
function reverseArray(arr) {}

/**
 * Devuelve indice de un elemento dentro de una array.
 * @param {arr} array - una array.
 * @param {element} no espicificado el tipo - un elemento dentro de la array.
 * @returns {number} - Retorna el indice del elemento "element" de la array "arr".
 */
function findIndexOfElement(arr, element) {}

/**
 * Devuelve una array mergeada de dos arrays.
 * @param {arr1} array - una array.
 * @param {arr2} array - una array.
 * @returns {array} - Retorna una única array que contenga todos los elementos de ambas arrays.
 */
function mergeArrays(arr1, arr2) {}

/**
 * Devuelve una array ordenada.
 * @param {arr} array - una array.
 * @returns {array} - Retorna una array ordendada ascendentamente segun los elementos de la array "arr".
 */
function sortArray(arr) {}

export {findLargestNumber, filterEvenNumbers, reverseArray, findIndexOfElement, mergeArrays, sortArray}